# Backlog triase PHPStan per paket

Satu file = draf body satu GitHub issue (judul: `PHPStan <paket>: triase & baseline level <N>`).
Buat via UI github.com atau API; setelah issue terbuka, folder ini bisa dihapus.
Runner: `bash scripts/phpstan-package.sh <paket>` · Manifest level: `.phpstan/packages.txt`.
