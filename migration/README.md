# Kodhe Migration

<!-- TODO: README sebelumnya keliru (ter-copy dari package lain). Tulis ulang mengikuti doc/TEMPLATE_README.md. -->

Package migrasi skema database untuk `kodhe/database` (lihat `migration/src/`). Dokumentasi lengkap sedang disusun — sementara, lihat kelas di `src/` dan contoh pada `doc/database_README.md`.
